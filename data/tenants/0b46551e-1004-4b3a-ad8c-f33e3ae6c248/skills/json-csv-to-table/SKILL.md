---
name: json-csv-to-table
description: "Convert JSON or CSV data into a formatted table."
type: tool
---

# JSON/CSV to Table

Use this skill when you need to convert JSON data (array of objects) or CSV data into a readable table format (Markdown). The input should be a string containing the data. The output will be a Markdown table.
